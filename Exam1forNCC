const http = require('http');

const requestListener = function (req, res) {
  res.writeHead(200);
  res.end('<html><body><h1>My Name is Myo Khant Kyaw.</h1></body></html>');
}

const server = http.createServer(requestListener);
server.listen(8080);
